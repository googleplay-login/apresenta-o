/**
 * Identidade e estado do projeto exibidos na interface.
 *
 * A página inicial mostra estes valores em vez de afirmar coisas em texto solto:
 * assim existe UM lugar para atualizar quando uma etapa for concluída, e o texto
 * não pode divergir do estado real do projeto.
 */

/** Versão do pacote (acompanha `package.json`). */
export const VERSAO = '0.3.0'

/** Nome de exibição do projeto. */
export const NOME = 'Arquipélago Python'

/** Onde o desenvolvimento está agora, em uma linha. */
export const ETAPA_ATUAL =
  'Etapa 3 em andamento — o mundo 3D navegável e o ciclo de estudo da primeira ilha'
