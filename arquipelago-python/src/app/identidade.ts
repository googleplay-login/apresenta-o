/**
 * Identidade do que esta sendo exibido.
 *
 * A pagina inicial mostra estes valores em vez de afirmar funcionalidades no
 * texto solto: assim, quando uma etapa for concluida, existe UM lugar para
 * atualizar, e o texto nao pode divergir do estado real do projeto.
 */

/** Versao do pacote (acompanha `package.json`). */
export const VERSAO = '0.1.0'

/** Nome de exibicao do projeto. */
export const NOME = 'Arquipelago Python'

/** Onde o desenvolvimento esta agora, em uma linha. */
export const ETAPA_ATUAL = 'Etapa 1.1 - fundacao: projeto, tema visual e documentacao'
